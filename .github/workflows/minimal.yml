name: Minimal Test

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: |
          echo "Hello from GitHub Actions!"
          echo "Runner IP: $(curl -s https://api.ipify.org)"
          echo "Test complete"
